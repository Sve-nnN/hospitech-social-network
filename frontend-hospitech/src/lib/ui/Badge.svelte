/**
 * @fileoverview Badge component for labels and tags
 * @author Juan Carlos Angulo
 * @component Badge
 */

<script>
  /**
   * @typedef {'primary' | 'secondary' | 'success' | 'warning' | 'error' | 'info'} BadgeVariant
   */

  let { 
    variant = 'primary',
    class: className = '',
    children
  } = $props();

  const variantClasses = {
    primary: 'bg-primary-100 text-primary-700',
    secondary: 'bg-secondary-100 text-secondary-700',
    success: 'bg-green-100 text-green-700',
    warning: 'bg-yellow-100 text-yellow-700',
    error: 'bg-red-100 text-red-700',
    info: 'bg-blue-100 text-blue-700'
  };
</script>

<span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium {variantClasses[variant]} {className}">
  {@render children()}
</span>
